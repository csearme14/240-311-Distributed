//คำนวณพื้นที่สามเหลี่ยม
let high = 4;
let base = 15;
var sum;
    high=parseInt(high);
    base=parseInt(base);
    console.log(sum = "Triangle area : "+  "1/2" + "*" + high + "*" + base + "="+(high*base*0.5))
